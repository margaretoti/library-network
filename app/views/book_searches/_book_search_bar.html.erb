<%= form_tag(book_search_path, method: :get, id: 'search_form') do %>
  <%= text_field_tag :search, params[:search], placeholder: t('book_searches.book_search_bar.placeholder') %>
  <%= submit_tag t('book_searches.book_search_bar.submit'), name: nil %>
<% end %>

<%= link_to t('book_searches.book_search_bar.reset'), books_path %>
